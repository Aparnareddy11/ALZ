# name: Deploy AKS with AVM Modules

# on:
#   push:
#     branches:
#       - main
#       - develop
#     paths:
#       - 'main.tf'
#       - 'providers.tf'
#       - 'variables.tf'
#       - 'AKS/**'
#       - '.github/workflows/deploy-aks.yml'
#   pull_request:
#     branches:
#       - main
#       - develop
#     paths:
#       - 'main.tf'
#       - 'providers.tf'
#       - 'variables.tf'
#       - 'AKS/**'
#   workflow_dispatch:

# env:
#   TF_VERSION: '1.9'
#   TF_WORKING_DIR: '.'
#   TF_IN_AUTOMATION: 'true'

# permissions:
#   contents: read
#   id-token: write

# jobs:
#   terraform-plan:
#     runs-on: ubuntu-latest
#     name: Terraform Plan
#     outputs:
#       plan-exists: ${{ steps.plan.outputs.exitcode }}
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Azure Login
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#           auth-type: SERVICE_PRINCIPAL

#       - name: Terraform Format Check
#         id: fmt
#         run: terraform fmt -check -recursive
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         continue-on-error: true

#       - name: Terraform Initialize (AVM Modules)
#         id: init
#         run: terraform init -upgrade
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         env:
#           TF_LOG: INFO

#       - name: Terraform Validate
#         id: validate
#         run: terraform validate
#         working-directory: ${{ env.TF_WORKING_DIR }}

#       - name: Terraform Plan
#         id: plan
#         run: |
#           terraform plan -out=tfplan -no-color -detailed-exitcode || EXIT_CODE=$?
#           echo "exitcode=${EXIT_CODE:-0}" >> $GITHUB_OUTPUT
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         continue-on-error: true

#       - name: Upload Terraform Plan
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: terraform-plan
#           path: ${{ env.TF_WORKING_DIR }}/tfplan
#           retention-days: 7

#       - name: Comment PR with Plan
#         if: github.event_name == 'pull_request' && always()
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const exitCode = '${{ steps.plan.outputs.exitcode }}';
#             const status = exitCode === '0' ? '✅ No changes' : exitCode === '2' ? '📝 Changes detected' : '❌ Plan failed';
#             github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: `## Terraform Plan - AVM Modules\n\n${status}\n\n**Deployment includes:** ALZ Pattern + AKS Configuration`
#             });

#   terraform-apply:
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#     runs-on: ubuntu-latest
#     name: Terraform Apply
#     needs: terraform-plan
#     environment: production
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Azure Login
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#           auth-type: SERVICE_PRINCIPAL

#       - name: Terraform Initialize (AVM Modules)
#         id: init
#         run: terraform init -upgrade
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         env:
#           TF_LOG: INFO

#       - name: Download Terraform Plan
#         uses: actions/download-artifact@v4
#         with:
#           name: terraform-plan
#           path: ${{ env.TF_WORKING_DIR }}

#       - name: Terraform Apply
#         id: apply
#         run: |
#           echo "Starting ALZ + AKS deployment with AVM modules..."
#           terraform apply -no-color tfplan
#           echo "apply_status=success" >> $GITHUB_OUTPUT
#         working-directory: ${{ env.TF_WORKING_DIR }}

#       - name: Capture Terraform Outputs
#         id: outputs
#         if: success()
#         run: terraform output -json > terraform-outputs.json
#         working-directory: ${{ env.TF_WORKING_DIR }}

#       - name: Upload Terraform Outputs
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: terraform-outputs
#           path: ${{ env.TF_WORKING_DIR }}/terraform-outputs.json
#           retention-days: 30

#       - name: Display Deployment Summary
#         if: always()
#         run: |
#           echo "## AVM Modules Deployed"
#           echo "- Azure Landing Zone (ALZ) Pattern Module"
#           echo "- AVM Utility Modules (Regions)"
#           echo "- AKS Configuration"
#         working-directory: ${{ env.TF_WORKING_DIR }}

#   aks-plan:
#     runs-on: ubuntu-latest
#     name: AKS Terraform Plan
#     outputs:
#       plan-exists: ${{ steps.plan.outputs.exitcode }}
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Azure Login
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#           auth-type: SERVICE_PRINCIPAL

#       - name: Terraform Format Check (AKS)
#         id: fmt
#         run: terraform fmt -check -recursive
#         working-directory: AKS
#         continue-on-error: true

#       - name: Terraform Initialize (AKS)
#         id: init
#         run: terraform init -upgrade
#         working-directory: AKS
#         env:
#           TF_LOG: INFO

#       - name: Terraform Validate (AKS)
#         id: validate
#         run: terraform validate
#         working-directory: AKS

#       - name: Terraform Plan (AKS)
#         id: plan
#         run: |
#           terraform plan -out=tfplan -no-color -detailed-exitcode || EXIT_CODE=$?
#           echo "exitcode=${EXIT_CODE:-0}" >> $GITHUB_OUTPUT
#         working-directory: AKS
#         continue-on-error: true

#       - name: Upload AKS Terraform Plan
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: aks-terraform-plan
#           path: AKS/tfplan
#           retention-days: 7

#       - name: Comment PR with AKS Plan
#         if: github.event_name == 'pull_request' && always()
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const exitCode = '${{ steps.plan.outputs.exitcode }}';
#             const status = exitCode === '0' ? '✅ No changes' : exitCode === '2' ? '📝 Changes detected' : '❌ Plan failed';
#             github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: `## AKS Terraform Plan\n\n${status}\n\n**AKS Configuration:** Cluster customization and networking`
#             });

#   aks-apply:
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#     runs-on: ubuntu-latest
#     name: AKS Terraform Apply
#     needs: [terraform-apply, aks-plan]
#     environment: production
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Azure Login
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#           auth-type: SERVICE_PRINCIPAL

#       - name: Terraform Initialize (AKS)
#         id: init
#         run: terraform init -upgrade
#         working-directory: AKS
#         env:
#           TF_LOG: INFO

#       - name: Download AKS Terraform Plan
#         uses: actions/download-artifact@v4
#         with:
#           name: aks-terraform-plan
#           path: AKS

#       - name: Terraform Apply (AKS)
#         id: apply
#         run: |
#           echo "Starting AKS deployment..."
#           terraform apply -no-color tfplan
#           echo "apply_status=success" >> $GITHUB_OUTPUT
#         working-directory: AKS

#       - name: Capture AKS Terraform Outputs
#         id: outputs
#         if: success()
#         run: terraform output -json > aks-outputs.json
#         working-directory: AKS

#       - name: Upload AKS Terraform Outputs
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: aks-terraform-outputs
#           path: AKS/aks-outputs.json
#           retention-days: 30

#       - name: Display AKS Deployment Summary
#         if: always()
#         run: |
#           echo "## AKS Deployment Complete"
#           echo "- AKS Cluster configured"
#           echo "- Node pools and networking ready"
#           echo "- RBAC and access controls applied"
#         working-directory: AKS

#   validate-deployment:
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#     runs-on: ubuntu-latest
#     name: Validate AVM Module Deployment
#     needs: [terraform-apply, aks-apply]
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Azure Login
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#           auth-type: SERVICE_PRINCIPAL

#       - name: Terraform Initialize
#         run: terraform init -upgrade
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         env:
#           TF_LOG: INFO

#       - name: Verify ALZ Deployment
#         id: verify-alz
#         run: |
#           echo "Verifying Azure Landing Zone (AVM Pattern Module) deployment..."
#           terraform state list | grep -i alz || echo "Warning: ALZ resources not found in state"
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         continue-on-error: true

#       - name: Verify Resources
#         id: verify-resources
#         run: |
#           echo "Checking deployed resources..."
#           RESOURCE_COUNT=$(terraform state list | wc -l)
#           echo "Total resources deployed: $RESOURCE_COUNT"
#           echo "resource_count=$RESOURCE_COUNT" >> $GITHUB_OUTPUT
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         continue-on-error: true

#       - name: Terraform Initialize (AKS)
#         run: terraform init -upgrade
#         working-directory: AKS
#         env:
#           TF_LOG: INFO

#       - name: Verify AKS Deployment
#         id: verify-aks
#         run: |
#           echo "Verifying AKS deployment..."
#           terraform state list | grep -i aks || echo "Warning: AKS resources not found in state"
#         working-directory: AKS
#         continue-on-error: true

#       - name: Verify AKS Cluster Resources
#         id: verify-aks-resources
#         run: |
#           echo "Checking deployed AKS resources..."
#           AKS_RESOURCE_COUNT=$(terraform state list | wc -l)
#           echo "AKS resources deployed: $AKS_RESOURCE_COUNT"
#           echo "aks_resource_count=$AKS_RESOURCE_COUNT" >> $GITHUB_OUTPUT
#         working-directory: AKS
#         continue-on-error: true

#       - name: Verify Azure Resources
#         run: |
#           echo "Verifying Azure resources..."
#           az resource list --output table || echo "No resources found or error querying"
#         continue-on-error: true

#       - name: Deployment Verification Summary
#         if: always()
#         run: |
#           echo "## ✅ Complete Deployment Verification"
#           echo ""
#           echo "**ALZ Components:**"
#           echo "- ✓ Azure Landing Zone (ALZ) Pattern"
#           echo "- ✓ AVM Utility Modules"
#           echo ""
#           echo "**AKS Components:**"
#           echo "- ✓ AKS Cluster Configuration"
#           echo "- ✓ Node Pools"
#           echo "- ✓ Networking & RBAC"
#           echo ""
#           echo "**Next Steps:**"
#           echo "1. Review Terraform outputs for resource details"
#           echo "2. Access AKS cluster using: az aks get-credentials"
#           echo "3. Verify networking and RBAC configurations"
